name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v1
      - name: Print GITHUB_WORKSPACE variable
        run: echo "${GITHUB_WORKSPACE}"
      - uses: ./self-hosted-runner-action-a
        with:
          RITM: "/Users/matteofoiadelli/Documents/Development/Amplifon/GitHub/OracleScripts/202204/RITM0069070"
