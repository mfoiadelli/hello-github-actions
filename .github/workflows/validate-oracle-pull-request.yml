name: Validate Oracle Pull Request
on: 
  pull_request:
    branches:    
      - main
    
jobs:
  check-files-names:
    name: Check Files Names
    runs-on: self-hosted
    
    steps:
      - name: Check Names
        id: checkNames
        run: |
          ./testShell.sh
        working-directory:
          ${{github.workspace}}/validate-oracle-pullrequest
          
