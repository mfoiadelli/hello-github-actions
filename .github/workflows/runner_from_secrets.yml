name: Runner From Secrets
on: 
  workflow_dispatch:
    inputs:
      environment:
        type: environment
        required: true
        default: 'TEST'
      test:
        type: string
        required: true
    
jobs:
  execute_step:
    environment: ${{github.event.inputs.environment}}
    name: ECHO TEST
    runs-on: ${{ secrets.RUNNER_LABEL }}
    steps:
      - name: echo
        id: echoTest
        run: |
          echo ${{github.event.inputs.test}}
          echo ${{ secrets.RUNNER_LABEL }}
          
          error=0
          
          exit $error
