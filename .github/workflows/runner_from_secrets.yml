name: Runner From Secrets
on: 
  workflow_dispatch:
    inputs:
      environment:
        type: environment
        required: true
        default: 'TEST'
      test:
        type: string
        required: true
    
jobs:
  execute_step:
    name: ECHO TEST
    environment: ${{github.event.inputs.environment}}
    runs-on: ${{github.event.inputs.test}}
    steps:
      - name: echo
        id: echoTest
        run: |
          echo ${{github.event.inputs.test}}
          echo ${{ secrets.RUNNEL_LABEL }}
          
          error=0
          
          exit $error
